# Disable greylist checks when sender authentication already passed.
settings {
  whitelist_signed {
    priority = high;
    expression = "R_DKIM_ALLOW | R_SPF_ALLOW | DMARC_POLICY_ALLOW";
    apply {
      symbols_disabled = ["GREYLIST_CHECK", "GREYLIST_SAVE"];
    }
  }
}
